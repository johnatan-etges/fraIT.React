import styled from 'styled-components';

export const Container = styled.div`
    #fra {
        fill: ${({theme}) => theme.contrast};
    }

    #it {
        fill: ${({theme}) => theme.logoIT};
    }
  
`;
